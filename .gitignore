**/.DS_Store
node_modules/
cdk.out/
*.js
!jest.config.js
*.d.ts
.cdk.staging/
.env

# Local account-number -> environment mapping; never commit.
environments.json

# CloudFront signing key pairs (private keys especially); never commit.
keys/

# Per-env stack outputs (contain live IPs/domains); regenerated each deploy.
deployment/outputs/

# Local private artifacts pushed to the deployment bucket; never commit.
private-config/
